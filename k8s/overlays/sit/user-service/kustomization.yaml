apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sit-user-service

resources:
  - ../../../base/user-service
  - namespace.yaml

patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: user-service
      spec:
        template:
          spec:
            containers:
              - name: user-service
                env:
                  - name: ENV_NAME
                    value: "sit"
    target:
      kind: Deployment
      name: user-service

