apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels:
    app: product-service
spec:
  selector:
    app: product-service
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
