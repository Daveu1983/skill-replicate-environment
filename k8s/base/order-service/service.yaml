apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
